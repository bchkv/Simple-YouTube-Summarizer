test with several yt videos

tweak the prompt

output summary.txt to the current dir

render a basic readme

better way to print out the summary?

optionally transcript locally with whisper

compare different whisper implementations

optionally modify the prompt